## REST FastAPI con conexión vía ORM

### Instrucciones
1. Crear entorno virtual 
2. Instalar dependencias: ``` pip install requirements.txt ```
3. Correr servidor: 2. ``` uvicorn main:app --reload --port xxxx ```